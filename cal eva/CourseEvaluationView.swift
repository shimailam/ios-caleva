import SwiftUI
struct CourseEvaluationView: View {
    var courseName: String
    
    var body: some View {
        // Add the UI components for the course evaluation
        Text("Evaluations for \(courseName)")
        // Add more UI elements as needed
    }
}
